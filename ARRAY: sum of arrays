//sum of arrays*****************

#include<stdio.h>

void main()
{
    int a[5],i,sum=0;
    printf("Enter the element of array\n");
    for(i=0;i<=4;i++)
    scanf("%d",&a[i]);

     for(i=0;i<=4;i++)
     sum=sum + a[i];
    printf("sum=%d",sum);
}
