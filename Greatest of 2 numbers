#include<stdio.h>

void main()
{
    int a,b,max;
    printf("Enter a and b");
    scanf("%d %d",&a,&b);

    // (a>b)?(max=a):(max=b);

    // max=(a>b)?(a):(b);

    (a>b)?(printf("A is greater")):(printf("B is greater"));


    printf("\n Maximum= %d",max);

}
